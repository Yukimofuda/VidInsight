# GitHub setup

Recommended repository: `VidInsight` (private during development if desired).

```bash
git remote add origin https://github.com/<OWNER>/VidInsight.git
git push -u origin main
git push -u origin develop
```

Create milestones: Baseline Ready (9/5), Feature Freeze (9/8), Release Candidate (9/10), Acceptance (9/11), Final Documentation (9/13).

Use labels: `P0`, `P1`, `P2`, `feature`, `ai`, `backend`, `frontend`, `bug`, `test`, `documentation`.
